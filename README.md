# mesheryctl-axi

Agent-ergonomic AXI wrapper around `mesheryctl`.

```bash
npx -y mesheryctl-axi
```

Requires `mesheryctl` installed and authenticated. See branch `feat/mesheryctl-axi-v1` for the full v1 implementation.
